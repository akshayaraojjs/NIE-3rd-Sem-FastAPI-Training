meta {
  name: 19 Teacher Lists All Students
  type: http
  seq: 19
}

get {
  url: {{base_url}}/students
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{teacher_token}}
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}

docs {
  19 Teacher Lists All Students.
  Endpoint: GET {{base_url}}/students
}

settings {
  encodeUrl: true
  timeout: 0
}
