meta {
  name: 20 Teacher Gets Ananya Iyer
  type: http
  seq: 20
}

get {
  url: {{base_url}}/students/{{ananya_id}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{teacher_token}}
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}

docs {
  20 Teacher Gets Ananya Iyer.
  Endpoint: GET {{base_url}}/students/{{ananya_id}}
}

settings {
  encodeUrl: true
  timeout: 0
}
