meta {
  name: 29 Login Wrong Password
  type: http
  seq: 29
}

post {
  url: {{base_url}}/login
  body: text
  auth: none
}

headers {
  Content-Type: application/x-www-form-urlencoded
}

body:text {
  username=aarav_admin&password=wrongpass
}

tests {
  test("Status is 401", function() {
    expect(res.status).to.equal(401);
  });
}

docs {
  29 Login Wrong Password.
  Endpoint: POST {{base_url}}/login
}

settings {
  encodeUrl: true
  timeout: 0
}
