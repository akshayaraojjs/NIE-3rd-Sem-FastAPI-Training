meta {
  name: 50 Admin Deletes Aarav Malhotra
  type: http
  seq: 50
}

delete {
  url: {{base_url}}/students/{{aarav_id}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{admin_token}}
}

tests {
  test("Status is 204", function() {
    expect(res.status).to.equal(204);
  });
}

docs {
  50 Admin Deletes Aarav Malhotra.
  Endpoint: DELETE {{base_url}}/students/{{aarav_id}}
}

settings {
  encodeUrl: true
  timeout: 0
}
