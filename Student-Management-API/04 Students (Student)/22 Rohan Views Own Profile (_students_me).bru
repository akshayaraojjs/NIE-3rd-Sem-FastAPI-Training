meta {
  name: 22 Rohan Views Own Profile (/students/me)
  type: http
  seq: 22
}

get {
  url: {{base_url}}/students/me
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{student_token}}
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
  test("Rohan profile", function() {
    expect(res.body.name).to.equal("Rohan Gupta");
  });
}

docs {
  22 Rohan Views Own Profile (/students/me).
  Endpoint: GET {{base_url}}/students/me
}

settings {
  encodeUrl: true
  timeout: 0
}
