meta {
  name: 24 Rohan Views Own Record By ID
  type: http
  seq: 24
}

get {
  url: {{base_url}}/students/{{rohan_id}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{student_token}}
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}

docs {
  24 Rohan Views Own Record By ID.
  Endpoint: GET {{base_url}}/students/{{rohan_id}}
}

settings {
  encodeUrl: true
  timeout: 0
}
