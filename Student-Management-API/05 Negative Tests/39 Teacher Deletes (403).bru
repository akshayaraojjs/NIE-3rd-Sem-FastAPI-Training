meta {
  name: 39 Teacher Deletes (403)
  type: http
  seq: 39
}

delete {
  url: {{base_url}}/students/{{rohan_id}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{teacher_token}}
}

tests {
  test("Status is 403", function() {
    expect(res.status).to.equal(403);
  });
}

docs {
  39 Teacher Deletes (403).
  Endpoint: DELETE {{base_url}}/students/{{rohan_id}}
}

settings {
  encodeUrl: true
  timeout: 0
}
