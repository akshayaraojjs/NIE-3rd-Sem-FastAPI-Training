meta {
  name: 37 Rohan Views Isha's Record (403)
  type: http
  seq: 37
}

get {
  url: {{base_url}}/students/{{isha_id}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{student_token}}
}

tests {
  test("Status is 403", function() {
    expect(res.status).to.equal(403);
  });
}

docs {
  37 Rohan Views Isha's Record (403).
  Endpoint: GET {{base_url}}/students/{{isha_id}}
}

settings {
  encodeUrl: true
  timeout: 0
}
