meta {
  name: 26 Isha Updates Own Record
  type: http
  seq: 26
}

put {
  url: {{base_url}}/students/{{isha_id}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{student2_token}}
}

body:json {
  {
    "course": "VLSI Design"
  }
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}

docs {
  26 Isha Updates Own Record.
  Endpoint: PUT {{base_url}}/students/{{isha_id}}
}

settings {
  encodeUrl: true
  timeout: 0
}
