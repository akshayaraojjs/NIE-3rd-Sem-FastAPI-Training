meta {
  name: 40 Student Deletes (403)
  type: http
  seq: 40
}

delete {
  url: {{base_url}}/students/{{isha_id}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{student_token}}
}

tests {
  test("Status is 403", function() {
    expect(res.status).to.equal(403);
  });
}

docs {
  40 Student Deletes (403).
  Endpoint: DELETE {{base_url}}/students/{{isha_id}}
}

settings {
  encodeUrl: true
  timeout: 0
}
