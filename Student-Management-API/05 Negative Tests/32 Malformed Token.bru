meta {
  name: 32 Malformed Token
  type: http
  seq: 32
}

get {
  url: {{base_url}}/students
  body: none
  auth: none
}

headers {
  Authorization: Bearer not.a.real.jwt
}

tests {
  test("Status is 401", function() {
    expect(res.status).to.equal(401);
  });
}

docs {
  32 Malformed Token.
  Endpoint: GET {{base_url}}/students
}

settings {
  encodeUrl: true
  timeout: 0
}
