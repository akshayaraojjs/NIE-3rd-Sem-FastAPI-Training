meta {
  name: 33 Teacher Creates Student (403)
  type: http
  seq: 33
}

post {
  url: {{base_url}}/students
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{teacher_token}}
}

body:json {
  {
    "name": "Kabir Singh",
    "age": 20,
    "course": "Physics"
  }
}

tests {
  test("Status is 403", function() {
    expect(res.status).to.equal(403);
  });
}

docs {
  33 Teacher Creates Student (403).
  Endpoint: POST {{base_url}}/students
}

settings {
  encodeUrl: true
  timeout: 0
}
