meta {
  name: 45 Bad user_id Format (400)
  type: http
  seq: 45
}

post {
  url: {{base_url}}/students
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{admin_token}}
}

body:json {
  {
    "name": "Kabir Singh",
    "age": 20,
    "course": "Physics",
    "user_id": "not-an-objectid"
  }
}

tests {
  test("Status is 400", function() {
    expect(res.status).to.equal(400);
  });
}

docs {
  45 Bad user_id Format (400).
  Endpoint: POST {{base_url}}/students
}

settings {
  encodeUrl: true
  timeout: 0
}
