meta {
  name: 34 Student Lists All (403)
  type: http
  seq: 34
}

get {
  url: {{base_url}}/students
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{student_token}}
}

tests {
  test("Status is 403", function() {
    expect(res.status).to.equal(403);
  });
}

docs {
  34 Student Lists All (403).
  Endpoint: GET {{base_url}}/students
}

settings {
  encodeUrl: true
  timeout: 0
}
